# Supabase (from Dashboard → Settings → API)
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<publishable-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-secret-key>
# JWT verification: with SUPABASE_URL the server uses JWKS (no secret needed for asymmetric signing keys).
# Optional – only for legacy symmetric (HS256) projects: use the full JWT Secret here, NOT the Key Id (kid).
# SUPABASE_JWT_SECRET=<full-jwt-secret>

# OpenAI (required for workflow generation)
OPENAI_API_KEY=<your-openai-api-key>
# Optional: model override (default o4-mini)
# OPENAI_MODEL=o4-mini

# Optional: server port (default 3000)
# PORT=3000

# Optional: when running npm run create-user, set a password for the new user (otherwise one is generated and printed)
# CREATE_USER_PASSWORD=your-secure-password

# Entry gate: Supabase Auth (email + password). No app-level password; auth is via Supabase.
